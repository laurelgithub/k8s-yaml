kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mongo-storage-mongo-2
  namespace: mongo
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
  storageClassName: nfs2
